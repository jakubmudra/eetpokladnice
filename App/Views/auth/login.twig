<div class="container pt-5">
    <div class="row">
        <div class="col-10 offset-1 col-md-4 offset-md-4 text-center">
            <h1>{{ __("app-name") }}</h1>
            <form method="POST" class="mt-5">
                <div class="form-group">
                    <input type="number" name="code" autocomplete="off" class="form-control text-center" placeholder="{{ __("enter-usercode") }}">
                </div>
                <div class="form-group">
                    <input type="password" name="password" class="form-control text-center"  placeholder="{{ __("enter-pincode") }}">
                </div>
                <button type="submit" class="btn btn-primary w-100 py-3">Log me in</button>
            </form>
        </div>
    </div>
</div>
