<div class="col sale-item">
    <div class="sale-item--inner active {{ item.data.color }}" data-x="{{ item.x }}" data-y="{{ item.y }}" onclick="{{ item.data.jsAction }}">
        <p>{{ item.data.displayName }}</p>
    </div>
</div>
